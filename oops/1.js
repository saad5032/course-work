console.log(this);
// this is a pointer to global object
// try on terminal : node + enter + this
console.log(this=={});
// this is a pointer to global object where as {} empty obj is pointing to different location so false
